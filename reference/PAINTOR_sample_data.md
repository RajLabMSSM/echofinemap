# Find PAINTOR sample data

Find PAINTOR sample data

## Usage

``` r
PAINTOR_sample_data(
  annot_dir = system.file("tools", "PAINTOR_V3.0", "SampleData", package = "echofinemap"),
  pattern = "\\.annotations",
  recursive = TRUE,
  read = FALSE,
  verbose = TRUE
)
```

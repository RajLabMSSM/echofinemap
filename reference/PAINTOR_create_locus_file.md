# Create QTL locus file for PAINTOR

Create QTL locus file for PAINTOR

## Usage

``` r
PAINTOR_create_locus_file(
  dat_merged,
  locus_dir,
  PT_results_path,
  NA_method = c("drop", "fill"),
  verbose = TRUE
)
```

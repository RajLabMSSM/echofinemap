# Remove duplicate SNPs

Remove any duplicate SNPs and rewrite the file to disk.

## Usage

``` r
remove_dup_snps(path, remove_dup = TRUE, verbose = TRUE)
```

## Arguments

- path:

  Path to file.

- remove_dup:

  Remove duplicate SNPs from the file.

- verbose:

  Print messages.

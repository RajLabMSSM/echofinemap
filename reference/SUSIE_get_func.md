# Get susieR fine-mapping function

The authors of susieR merged `susie_ss` and `susie_bhat` into
`susie_suff_stat` in 11/2019. They then changed `susie_ss` to
`susie_rss`.

## Usage

``` r
SUSIE_get_func(verbose = TRUE)
```

## Arguments

- verbose:

  Print messages.
